# DOC_ROOT prefixes absolute static resource URLs in example templates.
# kagami sets DOC_ROOT to working dir by default.
# Uncomment to make URLs start from /.
#unset DOC_ROOT

# these are recommended if you want to generate a valid RSS feed
# only SITE_HOSTNAME is required though
SITE_TITLE='Kagami Example Template'
SITE_DESCRIPTION="A sample template built from $VERSION"
SITE_HOSTNAME='https://sub.example.name'

# current year
YEAR="$(date '+%Y')"

# multi-line macros
ANNOUNCEMENT='<div style="background-color: #8800883C; margin: 10px; text-align: center;">

<h1>Did you know you can use multi-line macros with inline HTML?</h1>
<p>Check out <code>.kagami/macros</code> for ideas.</p>

</div>'
