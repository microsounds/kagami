# {DOC_ROOT} prefixes URLs pointing to static resources on your own site,
# such as stylesheets, images and scripts. kagami sets this to the
# working dir by default, uncomment to deploy on a webserver.
#unset DOC_ROOT
SITENAME='My COOL Website'
